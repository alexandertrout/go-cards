package main

//data type is infered from the right of the := / This is only ever used when Declaring a NEW variable.

func main() {
	// cards := newDeckFromFile("my_cards")
	// cards.shuffle()
	// cards.print()
	// d := newDeck()
}
